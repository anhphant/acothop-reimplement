## irace scenario file for not-simple-2

## Path to the target-runner script (called for each configuration evaluation)
targetRunner = "target-runner.bat"

## Parameter space definition
parameterFile = "parameters.txt"

## Training instance list (one file path per line)
trainInstancesFile = "instances.txt"

## How many experiments irace can run in total
## Each experiment = 1 algorithm run on 1 instance
## Budget: 1000 runs * ~30s each = ~8h wall time
maxExperiments = 1000

## irace RNG seed
seed = 1234567

## Time limit per run (seconds) — must match --time-limit in target-runner
## Set to NA to use maxExperiments budget only
# maxTime = NA

## Minimum number of experiments before any elimination
# minNbSurvival = 2

## Number of parallel processes (set to number of CPU cores for speed)
## WARNING: set to 1 if your machine is memory-constrained (the exe uses large static arrays)
parallel = 1

## Log file
logFile = "irace.Rdata"

## Show progress

## irace uses RACE (statistical test) to eliminate bad configurations
## Default: t-test with confidence 0.05
# testType = "t-test"
# confidence = 0.95
